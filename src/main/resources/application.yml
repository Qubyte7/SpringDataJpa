spring:
  datasource:
    url: jdbc:postgresql://localhost:8080/springjpa
    username: postgres
    password: Mugisha@2007
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: postgresql
    show-sql: true
server:
  port: 8082